[build]
  command = "npm run build && mkdir -p .netlify/static"
  publish = ".netlify/static"
  functions = ".netlify/functions-internal"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200
